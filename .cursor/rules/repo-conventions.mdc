---
description: LLOVES School repo layout conventions
alwaysApply: true
---

# Repo conventions

```
frameworks/                 # shared ELC constants
lms/                        # Flask LMS app
  seeds/                    # verified expectations JSON
  sources/ontario-curriculum/  # Ministry PDFs
content-builder/            # isolated math lesson factory (not the LMS)
tools/math-game-show/       # live class tracker DB/UI (staff Attendance & Participation surface)
agents/                     # school-facing agent prompts
scripts/                    # syllabus + IMSCC helper scripts
.cursor/rules/ + skills/    # school-facing only
```

- Do **not** add `courses/` or commit `.imscc` files.
- Math content factory writes under `content-builder/` only. Do not use LMS sqlite (`lms/data/`, Fly `/data`) as its catalogue index. Parent agent integrates shared builder files; overlapping component work uses isolated Git worktrees.
- Module packs: Admin upload → `content_libraries` on `/data`.
- Put shared school rules under `frameworks/` or `.cursor/rules/`.
- Teaching content > app scaffolding. Docstrings required on new functions/methods.
- Do not commit unless Shawn asks.
- `tools/math-game-show/` is the staff **Attendance & Participation** tracker (mounted via `lms/school_db.py` + staff course tabs), not a separate product island. Keep the path; change product language in the staff UI.
- Workflow lanes / localhost / main-only deploy: `.cursor/rules/local-first-workflow.mdc`.
