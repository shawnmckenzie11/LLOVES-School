# MAP4C A3.3

Official Ministry sample problems / e.g. examples only.

recognize that the compound interest formula, A = P(1 + i) , is an example of an exponential function A(n) when P and i are constant, and of a linear function A(P) when i and n are constant

comparing the graphs generated with technology when different variables in a formula are set as constants

Which variable(s) in the formula V = πr h would you need to set as a constant to generate a linear equation? A quadratic equation? Explain why you can expect the relationship between the volume and the height to be linear when the radius is constant.
