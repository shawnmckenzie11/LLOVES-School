# MCV4U A1.6

Official Ministry sample problems / e.g. examples only.

f(x) = x , f(x) = x

for f(x) = x at x = 3, by determining by first simplifying as = 6 + h and then substituting the same values of h to give the same results
