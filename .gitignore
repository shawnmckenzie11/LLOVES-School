# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
node_modules/
!.cursor/

# Local / secrets
.env
.env.*
!.env.example

# Temporary
*.tmp
_preserve/

# Math Game Show local DB, uploads, and session logs
tools/math-game-show/data/

# LLOVES LMS sqlite, syllabus saves, logs
lms/data/
lms/.venv/

# Local dumps (volume clone, curriculum cache, MathNet). Never commit banks,
# contest dumps, or extracts from here — Drive authoring is the other clone.
.local-data/
.cursor/debug-*.log
.cursor/hooks/state/
.cursor/hooks/__pycache__/

# Never commit IMSCC packs (Admin uploads to Fly volume only)
*.imscc
**/imscc-unpacked/

# Content-builder downloaded sources and local search index (not LMS sqlite).
# Path patterns also listed in content-builder/.gitignore.
content-builder/cache/
content-builder/index/*.sqlite
content-builder/index/*.db
content-builder/index/*.duckdb
