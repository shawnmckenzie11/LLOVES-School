# MHF4U A1.4

Official Ministry sample problems / e.g. examples only.

use the statement 10 = 10 10 to deduce that log x + log y = log (xy)

use patterning to verify the quotient law for logarithms by evaluating expressions such as log 1000 - log 100 and then rewriting the answer as a logarithmic term to the same base
