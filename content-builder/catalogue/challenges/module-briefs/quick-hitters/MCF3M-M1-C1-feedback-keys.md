# MCF3M M1C1 — instant text feedback (soft keys)

Teacher-authored juice for the live student prompt/response shell.  
Runtime table: `lms/live_prompt_feedback.py` (Fly image may omit this folder).  
Local teacher DB only — no cloud sync of student responses.

**Never attach feedback to the Team Challenge stem.**  
Minds-On stays ephemeral / juice-only (`durable_store: false`).

Wonder clarity bar: **one short line**, no pile.

---

## Minds-On · linear-rate MC · soft key **A**

Waiting-room check-in (prior-module main idea). Item ids: `minds_on` / `meet-math`.

**Prompt:** A line has constant rate of change. Which best matches that?

| Choice | Line |
|---|---|
| **A** (key) | Same step, same change — that’s a constant rate. |
| B | A curve changes steepness as you go. Constant rate stays even. |
| C | Second differences are a quadratic tell. Linear rate is first differences. |
| D | Constant rate means every equal step changes y by the same amount. |

---

## CONS-1 · `a` · soft key **B**

| Choice | Line |
|---|---|
| A | The graph opens upward, so a is positive. |
| **B** (key) | The graph opens upward, so a is greater than 0. |
| C | If a were 0, this would not be a parabola. |
| D | You can see the opening on the graph — a must be positive. |

---

## CONS-2 · `c` · soft key **C**

| Choice | Line |
|---|---|
| A | The intercept is the origin, so c is 0 — not positive. |
| B | The intercept is the origin, so c is 0 — not negative. |
| **C** (key) | The y-intercept is at the origin, so c = 0. |
| D | The graph crosses the y-axis at the origin, so c is 0. |

---

## CONS-3 · `b` · soft key **C**

| Choice | Line |
|---|---|
| A | The graph has no sideways lean, so b is 0, not positive. |
| B | The graph has no sideways lean, so b is 0, not negative. |
| **C** (key) | The vertex is at the origin and the graph is symmetric about the y-axis, so b = 0. |
| D | Y-axis symmetry on this graph means b = 0. |

---

## CONS-4 · share / draw · soft share line (not a hard MC key)

**on_submit:** Name one feature you marked and the claim it supports.

---

## CONS-5 · share · soft share line (not a hard MC key)

**on_submit:** What is still unknown from this graph alone?

---

## Out of scope

- Team Challenge stem / Real-slice media ask
- C2 / C3 text-only challenges
- Gradebook writeback
