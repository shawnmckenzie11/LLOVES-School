# Writing comparison (parent notes)

Anonymous rewrites of the same opening, worked example, and feedback. Models were not named in the writer files.

| | Comparison A | Comparison B |
|---|---|---|
| Naturalness | Clear, a bit textbook | More spoken; basketball image |
| Precision | Strong on \(h=-2\), max of 4 | Same math, more scaffolded bullets |
| Continuity | “Then graph the function” skips the lesson’s check tool | Prediction is explicit; “Before we work…” is mild itinerary |
| Hook conflict | Generic path/arch — does not steal A3.3 | Basketball flight is A3.3-ish; hook-curator must choose |

**Parent pick for copywriter voice (until Shawn overrides):** B’s worked-example and feedback pattern; do **not** lock in basketball. Opener comes from the selected hook. Copywriter model stays `inherit` in the agent file; prefer the B pattern in `writer-reference/comparison-B.md` sections 2–3.

Shawn: say A, B, or a mix if you want the agent model pinned.
